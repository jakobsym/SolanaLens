const tokenRoutes = (fastify, options) => {

    // enter CA and in return you get total holders,
    // market cap, pair age, 
    fastify.get('/:tokenAddress', async(req, res) => {
    })

    // top 10 holders
    
}

export default tokenRoutes